[package]
name = "armature-redis"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Redis client integration for Armature - connection pooling, pub/sub, and DI-ready"
keywords = ["redis", "cache", "pubsub", "pool", "async"]
categories = ["database", "asynchronous", "caching"]

[dependencies]
# Redis client - aligned with other armature crates (redis 1.0, bb8-redis 0.26)
redis = { version = "1.3", features = ["tokio-comp", "connection-manager", "cluster-async"] }

# Connection pooling
bb8 = "0.9"
bb8-redis = "0.26"

# Async runtime
tokio = { version = "1.52", features = ["sync", "time"] }
async-trait = "0.1"
futures = "0.3"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error handling
thiserror = "2.0"

# Utilities
tracing = "0.1"
parking_lot = "0.12"
url = "2.5"
urlencoding = "2.1"

[dev-dependencies]
tokio = { version = "1.52", features = ["rt-multi-thread", "macros", "sync", "time", "net", "io-util", "fs", "test-util"] }
armature-testkit = { path = "../armature-testkit", features = ["containers"] }

[features]
default = []
# TLS support
tls = ["redis/tokio-rustls-comp"]
# All features
full = ["tls"]

